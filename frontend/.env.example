# YieldAgent backend URL (HTTP API + WebSocket)
NEXT_PUBLIC_AGENT_URL=http://localhost:3001

# WalletConnect project ID (for RainbowKit)
# Create one at https://cloud.walletconnect.com
NEXT_PUBLIC_WC_PROJECT_ID=yieldagent_demo

# Deployed contract addresses (filled after scripts/deploy.ts)
NEXT_PUBLIC_STRATEGY_MANAGER=
NEXT_PUBLIC_DECISION_LOGGER=
NEXT_PUBLIC_FOLLOW_VAULT_FACTORY=
